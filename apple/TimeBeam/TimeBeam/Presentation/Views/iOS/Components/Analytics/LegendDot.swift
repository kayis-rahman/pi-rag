// Extracted from AnalyticsView.swift

import SwiftUI
import Charts

    let color: Color
    var body: some View {
        Circle()
            .fill(color)
            .frame(width: 10, height: 10)
    }
}
