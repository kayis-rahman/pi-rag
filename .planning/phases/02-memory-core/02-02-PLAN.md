---
phase: 02-memory-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/synapse/memory/knowledgegraph/KnowledgeGraphService.java
  - app/src/main/java/com/synapse/memory/config/KnowledgeGraphConfig.java
  - app/src/test/java/com/synapse/memory/knowledgegraph/KnowledgeGraphServiceTest.java
autonomous: true
requirements: [MEM-05, MEM-06]
user_setup: []

must_haves:
  truths:
    - "Relationship edge can be stored with source, relation type, and target"
    - "Related concepts retrievable for any entity ID across relationship types"
    - "SQLite database initialized with graph_edges and graph_entities tables"
    - "Composite indexes on (source_type, source_id) and (target_type, target_id) for query performance"
  artifacts:
    - path: "app/src/main/java/com/synapse/memory/knowledgegraph/KnowledgeGraphService.java"
      provides: "SQLite triple store operations for knowledge graph"
      exports: ["storeRelationship(String source, String relation, String target)", "findRelatedConcepts(String entityId, int limit)"]
    - path: "app/src/main/java/com/synapse/memory/config/KnowledgeGraphConfig.java"
      provides: "SQLite connection pooling and schema initialization"
      exports: ["DataSource", "JdbcTemplate"]
    - path: "app/src/test/java/com/synapse/memory/knowledgegraph/KnowledgeGraphServiceTest.java"
      provides: "Unit tests for entity and relationship operations"
      min_lines: 50
  key_links:
    - from: "KnowledgeGraphService"
      to: "SQLite database (knowledge.db)"
      via: "JdbcTemplate connection"
      pattern: "JdbcTemplate.*execute"
    - from: "KnowledgeGraphService"
      to: "graph_edges table"
      via: "INSERT/SELECT operations"
      pattern: "INSERT INTO graph_edges"
    - from: "KnowledgeGraphConfig"
      to: "DataSource bean"
      via: "@Bean DataSource"
      pattern: "DriverManagerDataSource"
---

<objective>
Implement knowledge graph service for storing and querying semantic relationships as triple edges (source, relation, target). SQLite provides lightweight relational storage with indexed queries for entity connectivity.

Purpose: Enable relationship queries to find all concepts connected to an entity across configurable relation types.

Output: Functional KnowledgeGraphService with SQLite schema, entity/relationship storage, and query operations.
</objective>

<execution_context>
@/Users/kayisrahman/Documents/workspace/ideas/synapse/CLAUDE.md
@/Users/kayisrahman/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-memory-core/02-CONTEXT.md
@.planning/phases/02-memory-core/02-RESEARCH.md

# SQLite schema (from CONTEXT.md)
From 02-CONTEXT.md, the decision is to use SQLite triple store:
```
CREATE TABLE IF NOT EXISTS graph_edges (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source_type TEXT NOT NULL,  -- file, concept, conversation
    source_id TEXT NOT NULL,
    relation TEXT NOT NULL,      -- contains, references, documents, etc.
    target_type TEXT NOT NULL,
    target_id TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata TEXT  -- JSON
);

CREATE INDEX idx_source ON graph_edges(source_type, source_id);
CREATE INDEX idx_target ON graph_edges(target_type, target_id);
```

From build.gradle (already included):
- sqlite-jdbc:3.42.0.0
- spring-boot-starter-jdbc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KnowledgeGraphConfig for SQLite DataSource and JdbcTemplate</name>
  <files>app/src/main/java/com/synapse/memory/config/KnowledgeGraphConfig.java</files>
  <action>
Create a new Spring @Configuration class for knowledge graph SQLite setup.

Implementation requirements:

1. **DataSource bean:**
   - Use DriverManagerDataSource (Spring JDBC) pointing to SQLite database path from properties
   - Path: `${memory.knowledge.sqlite.path:/var/lib/synapse/knowledge.db}` (fallback to /var/lib/synapse)
   - Set jdbcUrl to `jdbc:sqlite:` + path
   - No connection pooling needed for SQLite (single-threaded writes)

2. **JdbcTemplate bean:**
   - Create @Bean JdbcTemplate(DataSource dataSource) returning new JdbcTemplate(dataSource)
   - This provides Spring abstraction for SQL execution

3. **Initialize schema on startup:**
   - Create @Bean InitializingBean that calls a private initializeSchema() method
   - Method uses JdbcTemplate to execute CREATE TABLE IF NOT EXISTS statements
   - Create graph_edges table with:
     - id (INTEGER PRIMARY KEY AUTOINCREMENT)
     - source_type, source_id (TEXT NOT NULL)
     - relation (TEXT NOT NULL)
     - target_type, target_id (TEXT NOT NULL)
     - created_at (TIMESTAMP DEFAULT CURRENT_TIMESTAMP)
     - metadata (TEXT for JSON)
   - Create two indexes:
     - idx_source ON (source_type, source_id)
     - idx_target ON (target_type, target_id)

4. **Error handling:**
   - Wrap schema initialization in try/catch; log errors but don't fail startup (schema may already exist)
   - Use DDL statements that check IF NOT EXISTS

5. **Configuration properties:**
   - Read path from @Value("${memory.knowledge.sqlite.path:/var/lib/synapse/knowledge.db}")
   - Use Spring Boot property resolution

Reference: Spring JDBC DataSource configuration guide. Keep it minimal and single-purpose.
  </action>
  <verify>
    <automated>
cd /Users/kayisrahman/Documents/workspace/ideas/synapse && \
./gradlew clean build -x test && \
ls -la /var/lib/synapse/knowledge.db 2>/dev/null || echo "DB will be created on first run"
    </automated>
  </verify>
  <done>
    - KnowledgeGraphConfig.java created and compiles
    - DataSource bean registered
    - JdbcTemplate bean registered
    - Schema initialization runs on startup
    - graph_edges table exists with indexes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement KnowledgeGraphService with SQLite triple store operations</name>
  <files>app/src/main/java/com/synapse/memory/knowledgegraph/KnowledgeGraphService.java</files>
  <behavior>
    - Store relationship: Insert triple (source_type, source_id, relation, target_type, target_id) into graph_edges
    - Find related concepts: Query all edges where source_id = ? or target_id = ?, return connected entity IDs
    - Support metadata: Store JSON metadata for relationships (optional but prepared)
    - Use indexed queries for performance: Leverage idx_source and idx_target
  </behavior>
  <action>
Uncomment and refactor the existing KnowledgeGraphService file (currently commented out in
`app/src/main/java/com/synapse/memory/knowledgegraph/KnowledgeGraphService.java`).

Key changes from existing stub:

1. **Replace constructor-based SQLite init with Spring injection:**
   - Remove direct DriverManager.getConnection(...) in constructor
   - Add @Autowired private JdbcTemplate jdbcTemplate;
   - Remove manual connection creation; use JdbcTemplate for all SQL operations

2. **Implement storeRelationship(String sourceType, String sourceId, String relation, String targetType, String targetId, Map<String, String> metadata):**
   - Use jdbcTemplate.update() to INSERT:
     ```
     INSERT INTO graph_edges (source_type, source_id, relation, target_type, target_id, metadata)
     VALUES (?, ?, ?, ?, ?, ?)
     ```
   - Serialize metadata Map to JSON string (use Jackson ObjectMapper or simple JSON)
   - Return KeyHolder to get auto-generated id if needed

3. **Implement findRelatedConcepts(String entityId, int limit) -> List<String>:**
   - Query graph_edges for bidirectional connections:
     ```
     SELECT DISTINCT CASE
       WHEN source_id = ? THEN target_id
       ELSE source_id
     END as connected_id
     FROM graph_edges
     WHERE source_id = ? OR target_id = ?
     ORDER BY created_at DESC
     LIMIT ?
     ```
   - Use RowMapper<String> to extract connected entity IDs
   - Return List<String> of connected entity IDs

4. **Implement queryRelationships(String sourceId, String relation, int limit) -> List<String>:**
   - Query specific relationship type:
     ```
     SELECT target_id FROM graph_edges WHERE source_id = ? AND relation = ? LIMIT ?
     ```
   - Returns targets of specific relation from source (not bidirectional)

5. **Error handling:**
   - Wrap JDBC operations in try/catch; throw RuntimeException on SQL errors (data loss severity)
   - Log warnings if queries return no results

6. **Use Spring conventions:**
   - @Service annotation, @Autowired for dependencies
   - JdbcTemplate for all SQL (not raw Connection)
   - Follow Spring Data JDBC patterns

7. **Lifecycle:**
   - No explicit shutdown needed (DataSource/JdbcTemplate managed by Spring)

Reference: Spring JDBC guide for update() and query() patterns. Use RowMapper for type-safe result mapping.
  </action>
  <verify>
    <automated>
cd /Users/kayisrahman/Documents/workspace/ideas/synapse && \
./gradlew test --tests 'com.synapse.memory.knowledgegraph.KnowledgeGraphServiceTest*' -i
    </automated>
  </verify>
  <done>
    - KnowledgeGraphService compiles without errors
    - storeRelationship() inserts edges into graph_edges table
    - findRelatedConcepts() returns connected entity IDs
    - queryRelationships() returns entities of specific relation type
    - Indexed queries execute without N+1 problems
    - Unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create KnowledgeGraphServiceTest with relationship storage and query tests</name>
  <files>app/src/test/java/com/synapse/memory/knowledgegraph/KnowledgeGraphServiceTest.java</files>
  <behavior>
    - Test store single relationship: Verify INSERT executes and data persists
    - Test find related concepts: Store edges, query for connected entities, verify results
    - Test bidirectional queries: Store A->B and verify both directions return results
    - Test relationship filtering: Query specific relation type, verify only matching edges returned
    - Test limit parameter: Store 5 edges, query with limit 3, verify exactly 3 returned
  </behavior>
  <action>
Create comprehensive unit tests for KnowledgeGraphService using JUnit 5 and Spring Test framework.

Test structure:

1. **Setup (@BeforeEach):**
   - Use Spring @SpringBootTest(webEnvironment = WebEnvironment.NONE) for integration
   - Inject KnowledgeGraphService and JdbcTemplate
   - Use test SQLite database (in-memory or temporary file)
   - Create tables via KnowledgeGraphConfig initialization

2. **Test: storeRelationship_InsertsEdgeIntoDB():**
   - Call storeRelationship("file", "UserService.java", "references", "file", "RepositoryInterface.java", null)
   - Query database directly: SELECT COUNT(*) FROM graph_edges WHERE source_id = ?
   - Assert count = 1

3. **Test: findRelatedConcepts_ReturnsBidirectionalConnections():**
   - Store 3 edges: A->B (references), B->C (contains), D->A (imports)
   - Call findRelatedConcepts("A", 10)
   - Assert returns B (forward) and D (backward), count = 2

4. **Test: queryRelationships_ReturnsSpecificRelationType():**
   - Store 3 edges: A->B (references), A->C (imports), A->D (references)
   - Call queryRelationships("A", "references", 10)
   - Assert returns [B, D], count = 2 (excludes imports)

5. **Test: findRelatedConcepts_RespectsLimit():**
   - Store 10 edges from same source
   - Call findRelatedConcepts("A", 5)
   - Assert returns exactly 5 results

6. **Test: findRelatedConcepts_EmptyForUnknownEntity():**
   - Don't store any edges for "unknown"
   - Call findRelatedConcepts("unknown", 10)
   - Assert returns empty list (not null)

Use Spring's test fixtures and assertion helpers. Keep tests isolated; each test should create its own test data.
  </action>
  <verify>
    <automated>
cd /Users/kayisrahman/Documents/workspace/ideas/synapse && \
./gradlew test --tests 'com.synapse.memory.knowledgegraph.KnowledgeGraphServiceTest' -i
    </automated>
  </verify>
  <done>
    - Test file exists with at least 5 test methods
    - All tests pass (store, find, query, limit, empty)
    - Test coverage >80% for KnowledgeGraphService
    - Bidirectional and unidirectional queries verified
    - SQLite interactions confirmed (indexed queries via EXPLAIN QUERY PLAN)
  </done>
</task>

</tasks>

<verification>
After execution:
1. Verify KnowledgeGraphConfig compiles: `gradle build -x test`
2. Verify KnowledgeGraphService compiles and runs tests: `gradle test --tests KnowledgeGraphServiceTest`
3. Check SQLite database created with schema: `sqlite3 /var/lib/synapse/knowledge.db ".schema"`
4. Verify composite indexes exist on (source_type, source_id) and (target_type, target_id)
</verification>

<success_criteria>
- KnowledgeGraphConfig created with DataSource and JdbcTemplate beans
- KnowledgeGraphService fully implemented (uncommented and refactored from stub)
- storeRelationship() inserts edges into graph_edges table
- findRelatedConcepts() returns bidirectional connections
- queryRelationships() returns unidirectional connections of specific type
- Unit tests verify all core behavior
- Gradle build passes with no compilation errors
- All tests pass
- MEM-05 (relationship edges stored) and MEM-06 (traverse knowledge graph) requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-memory-core/02-02-SUMMARY.md` with:
- Files modified (KnowledgeGraphService, KnowledgeGraphConfig, tests)
- Key implementation decisions (SQLite triple store, indexed queries, bidirectional traversal)
- Test coverage summary
- Integration point for Plan 03 (UnifiedMemoryService uses this service)
</output>
