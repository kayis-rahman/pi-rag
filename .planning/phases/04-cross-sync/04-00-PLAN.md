---
phase: 04-cross-sync
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerQueueTests.swift
  - apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerBackoffTests.swift
  - apple/TimeBeam/TimeBeamTests/UnitTests/Services/SyncFailureAlertTests.swift
  - apple/TimeBeam/TimeBeamTests/IntegrationTests/PushPayloadIntegrationTests.swift
  - apple/TimeBeam/TimeBeamTests/IntegrationTests/SetupAppSyncTests.swift
  - back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServicePushPayloadTest.java
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03, SYNC-04, SYNC-05, SYNC-06]
user_setup: []

must_haves:
  truths:
    - "TimerSyncManagerQueueTests.swift exists with test_enqueueAction_addsToQueue and test_drainActionQueue_replaysInOrder stubs"
    - "TimerSyncManagerBackoffTests.swift exists with test_syncFailure_incrementsBackoffInterval stub"
    - "SyncFailureAlertTests.swift exists with test_threeConsecutiveFailures_triggersAlert stub"
    - "PushPayloadIntegrationTests.swift exists with test_pushPayload_containsAllRequiredFields stub"
    - "SetupAppSyncTests.swift exists with test_setupApp_pullsTimerStateAfterAuth stub"
    - "TimerSyncServicePushPayloadTest.java exists with test_sendTimerSyncPush_includesAutoStartNextSession stub"
  artifacts:
    - path: "apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerQueueTests.swift"
      provides: "Test stubs for offline queue behavior (SYNC-02, SYNC-03)"
      contains: "class TimerSyncManagerQueueTests"
    - path: "apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerBackoffTests.swift"
      provides: "Test stubs for exponential backoff (SYNC-04)"
      contains: "class TimerSyncManagerBackoffTests"
    - path: "apple/TimeBeam/TimeBeamTests/UnitTests/Services/SyncFailureAlertTests.swift"
      provides: "Test stubs for sync failure alert UI (D-06)"
      contains: "class SyncFailureAlertTests"
    - path: "apple/TimeBeam/TimeBeamTests/IntegrationTests/PushPayloadIntegrationTests.swift"
      provides: "Test stubs for push delta apply (SYNC-05)"
      contains: "class PushPayloadIntegrationTests"
    - path: "apple/TimeBeam/TimeBeamTests/IntegrationTests/SetupAppSyncTests.swift"
      provides: "Test stubs for state restoration on relaunch (SYNC-06)"
      contains: "class SetupAppSyncTests"
    - path: "back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServicePushPayloadTest.java"
      provides: "Java unit tests for push payload completeness (SYNC-05)"
      contains: "class TimerSyncServicePushPayloadTest"
  key_links:
    - from: "TimerSyncManagerQueueTests.swift"
      to: "TimerSyncManager.swift actionQueue"
      via: "XCTest unit tests"
      pattern: "TimerSyncManagerQueueTests"
    - from: "TimerSyncServicePushPayloadTest.java"
      to: "PushNotificationService.java sendTimerSyncPush"
      via: "Mockito + JUnit 5 unit tests"
      pattern: "TimerSyncServicePushPayloadTest"
---

<objective>
Create Wave 0 test stub files for Phase 4 cross-sync. These stubs define the test contract before implementation runs — each stub compiles and fails (RED state). Implementation in Waves 1-3 will make them pass (GREEN).

Purpose: The research identified 6 test files as Wave 0 gaps. Writing them before implementation ensures test-driven delivery: plans 01-04 must satisfy these test contracts. Stubs are intentionally minimal — just XCTFail("not yet implemented") bodies and Java @Test @Disabled bodies. The stub structure (method names, parameter types) defines the behavioral contract for each phase requirement.

Output: 5 Swift test files + 1 Java test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-sync/04-CONTEXT.md
@.planning/phases/04-cross-sync/04-RESEARCH.md
@apple/TimeBeam/TimeBeamTests/UnitTests/PomodoroTimerUnitTests.swift
@apple/TimeBeam/TimeBeamTests/UnitTests/Services
@apple/TimeBeam/TimeBeamTests/IntegrationTests/TimerSyncIntegrationTests.swift
@back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServiceComprehensiveTest.java
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Swift unit test stubs for queue and backoff</name>
  <files>
    apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerQueueTests.swift
    apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerBackoffTests.swift
    apple/TimeBeam/TimeBeamTests/UnitTests/Services/SyncFailureAlertTests.swift
  </files>
  <read_first>
    - apple/TimeBeam/TimeBeamTests/UnitTests/PomodoroTimerUnitTests.swift (existing XCTest pattern — import statements, @MainActor usage, setUp/tearDown)
    - apple/TimeBeam/TimeBeamTests/UnitTests/Services (list existing files in this directory)
    - .planning/phases/04-cross-sync/04-RESEARCH.md (Validation Architecture section — test names listed in Wave 0 Gaps)
  </read_first>
  <behavior>
    - All 3 test files compile without errors
    - Each test method stub body calls XCTFail("not implemented") so tests fail by default (RED state)
    - Test class and method names match the contracts in RESEARCH.md Wave 0 Gaps
  </behavior>
  <action>
    Create TimerSyncManagerQueueTests.swift with class TimerSyncManagerQueueTests: XCTestCase covering:
    - test_enqueueAction_addsToQueue — verify enqueueAction appends to actionQueue array
    - test_enqueueAction_persistsToKeychain — verify persistActionQueue called on enqueue
    - test_queue_overflow_dropsOldest — verify maxQueueSize=50 limit drops first element
    - test_drainActionQueue_replaysInOrder — verify queued actions sent in FIFO order
    - test_drainActionQueue_requeuesOnConsecutiveFailures — verify re-queue after 3 failures
    - test_loadActionQueue_restoresFromKeychain — verify Keychain round-trip

    Create TimerSyncManagerBackoffTests.swift with class TimerSyncManagerBackoffTests: XCTestCase covering:
    - test_firstFailure_backoff30s — verify backoff interval is 30s after 1 failure
    - test_secondFailure_backoff60s — verify backoff interval is 60s after 2 failures
    - test_thirdFailure_backoff120s — verify backoff interval is 120s after 3 failures
    - test_fourthFailure_capped300s — verify backoff capped at 300s after 4+ failures
    - test_successResetsBackoff — verify consecutive failures counter resets to 0 on success

    Create SyncFailureAlertTests.swift with class SyncFailureAlertTests: XCTestCase covering:
    - test_twoFailures_noAlert — verify alert NOT shown after 2 consecutive failures
    - test_threeConsecutiveFailures_triggersAlert — verify isAlertVisible=true after 3 failures
    - test_manualRetry_resetsFailureCount — verify retry resets counter and hides alert
    - test_successAfterAlert_dismissesAlert — verify successful sync clears alert state

    All stubs use pattern: `func test_X() { XCTFail("not implemented — will be satisfied by Plan 02/03/04") }`
    Follow import pattern from existing XCTest files (import XCTest, @testable import TimeBeam).
  </action>
  <verify>
    <automated>cd apple/TimeBeam && xcodebuild -scheme "TimeBeam" -destination 'platform=macOS' build-for-testing 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 3 Swift test files exist and compile. xcodebuild build-for-testing succeeds. Each file has the XCTestCase class and stub test methods with XCTFail bodies.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Swift integration test stubs for push payload and state restoration</name>
  <files>
    apple/TimeBeam/TimeBeamTests/IntegrationTests/PushPayloadIntegrationTests.swift
    apple/TimeBeam/TimeBeamTests/IntegrationTests/SetupAppSyncTests.swift
  </files>
  <read_first>
    - apple/TimeBeam/TimeBeamTests/IntegrationTests/TimerSyncIntegrationTests.swift (existing integration test pattern)
    - .planning/phases/04-cross-sync/04-RESEARCH.md (Pattern 4: Push Delta Apply section for field names)
  </read_first>
  <behavior>
    - Both files compile without errors
    - Each stub calls XCTFail("not implemented") so tests fail by default (RED state)
  </behavior>
  <action>
    Create PushPayloadIntegrationTests.swift with class PushPayloadIntegrationTests: XCTestCase covering:
    - test_applyEventState_withNewerTimestamp_appliesState — verify state updates when push timestamp > local
    - test_applyEventState_withOlderTimestamp_skipsApply — verify state NOT updated when push timestamp <= local (SYNC-01)
    - test_applyEventState_readsAutoStartNextSession — verify autoStartNextSession comes from userInfo, not local fallback
    - test_applyEventState_readsShortBreaksCompleted — verify shortBreaksCompleted comes from userInfo, not local fallback
    - test_pushPayload_containsAllRequiredFields — verify push userInfo dict has: phase, remainingSeconds, isRunning, workDuration, breakDuration, longBreakDuration, autoStartNextSession, shortBreaksCompleted, deviceId, lastModifiedTimestamp (SYNC-05)

    Create SetupAppSyncTests.swift with class SetupAppSyncTests: XCTestCase covering:
    - test_setupApp_restoresAuthBeforePull — verify restoreSession() called before timer state pull
    - test_setupApp_pullsTimerStateAfterAuth — verify timer state pulled from backend after auth restore
    - test_setupApp_setsIsAppReadyLast — verify isAppReady = true set only after state pulled (SYNC-06)
    - test_appLaunch_timerStateMatchesBackend — verify local timer state matches pulled state on launch

    All stubs: `func test_X() { XCTFail("not implemented — will be satisfied by Plan 02/setupApp()") }`
  </action>
  <verify>
    <automated>cd apple/TimeBeam && xcodebuild -scheme "TimeBeam" -destination 'platform=macOS' build-for-testing 2>&1 | tail -10</automated>
  </verify>
  <done>
    Both integration test files exist and compile. xcodebuild build-for-testing succeeds. Files have XCTestCase class with stub test methods.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create Java unit test stubs for push payload backend</name>
  <files>
    back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServicePushPayloadTest.java
  </files>
  <read_first>
    - back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServiceComprehensiveTest.java (existing Java test pattern — @ExtendWith, @Mock, @InjectMocks, Mockito verify)
    - back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java (sendTimerSyncPush method signature)
    - back-end/src/main/java/com/sparkage/timebeam/infrastructure/persistence/TimerState.java (entity fields for test data setup)
  </read_first>
  <behavior>
    - File compiles with mvn test-compile
    - @Disabled stubs exist so they are skipped but show up as test entries
  </behavior>
  <action>
    Create TimerSyncServicePushPayloadTest.java in package com.sparkage.timebeam.application.service.

    Use @ExtendWith(MockitoExtension.class), @Mock PushNotificationService pushNotificationService, @Mock TimerState timerState.

    Add stubs with @Test @Disabled("not implemented — will be satisfied by Plan 01"):
    - test_sendTimerSyncPush_includesAutoStartNextSession — verify push payload String contains "autoStartNextSession"
    - test_sendTimerSyncPush_includesShortBreaksCompleted — verify push payload String contains "shortBreaksCompleted"
    - test_sendTimerSyncPush_includesLastModifiedTimestamp — verify push payload String contains "lastModifiedTimestamp"
    - test_convertActionToStateDto_readsAutoStartNextFromEntity — verify convertActionToStateDto calls state.getAutoStartNext()
    - test_convertActionToStateDto_readsShortBreaksCompletedFromEntity — verify convertActionToStateDto reads shortBreaksCompleted

    Use fail("not implemented") in each stub body to ensure RED state if @Disabled is ever removed.
  </action>
  <verify>
    <automated>cd back-end && mvn test-compile -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    TimerSyncServicePushPayloadTest.java exists and compiles. mvn test-compile exits 0. File has 5 @Disabled @Test stub methods.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None — Wave 0 creates test stubs only. No production code modified.

## STRIDE Threat Register
Not applicable for test-only wave.
</threat_model>

<verification>
1. All 5 Swift test files exist:
   - `test -f apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerQueueTests.swift`
   - `test -f apple/TimeBeam/TimeBeamTests/UnitTests/Services/TimerSyncManagerBackoffTests.swift`
   - `test -f apple/TimeBeam/TimeBeamTests/UnitTests/Services/SyncFailureAlertTests.swift`
   - `test -f apple/TimeBeam/TimeBeamTests/IntegrationTests/PushPayloadIntegrationTests.swift`
   - `test -f apple/TimeBeam/TimeBeamTests/IntegrationTests/SetupAppSyncTests.swift`
2. Java test exists: `test -f back-end/src/test/java/com/sparkage/timebeam/application/service/TimerSyncServicePushPayloadTest.java`
3. Swift builds: `cd apple/TimeBeam && xcodebuild -scheme "TimeBeam" -destination 'platform=macOS' build-for-testing 2>&1 | grep -E "BUILD SUCCEEDED|error:"`
4. Java compiles: `cd back-end && mvn test-compile -q`
</verification>

<success_criteria>
- 6 test stub files exist at the specified paths
- All Swift stubs compile (xcodebuild build-for-testing succeeds)
- Java stub compiles (mvn test-compile succeeds)
- Each stub has failing XCTFail/fail() bodies confirming RED state
- All 6 SYNC requirement IDs are referenced across the stubs
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-sync/04-00-SUMMARY.md`
</output>
