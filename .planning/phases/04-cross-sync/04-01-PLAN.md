---
phase: 04-cross-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java
  - back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java
autonomous: true
requirements: [SYNC-05]
user_setup: []

must_haves:
  truths:
    - "Push payload JSON includes autoStartNextSession field with correct boolean value from TimerState entity"
    - "Push payload JSON includes shortBreaksCompleted field with correct integer value from TimerState entity"
    - "convertActionToStateDto passes entity's autoStartNextSession value instead of hardcoded false"
  artifacts:
    - path: "back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java"
      provides: "Push payload with autoStartNextSession and shortBreaksCompleted fields"
      contains: "autoStartNextSession"
    - path: "back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java"
      provides: "convertActionToStateDto that reads autoStartNextSession from entity"
      contains: "getAutoStartNext()"
  key_links:
    - from: "PushNotificationService.java sendTimerSyncPush"
      to: "TimerStateDto payload fields"
      via: "String.format template"
      pattern: "autoStartNextSession.*shortBreaksCompleted"
    - from: "TimerSyncService.java convertActionToStateDto"
      to: "TimerState entity"
      via: "state.getAutoStartNext()"
      pattern: "getAutoStartNext"
---

<objective>
Fix the backend push notification payload to include all timer state fields required for correct cross-device delta apply.

Purpose: The current push payload template in PushNotificationService is missing autoStartNextSession and shortBreaksCompleted. The convertActionToStateDto method hardcodes false for autoStartNextSession. These gaps cause state divergence when the receiving device applies the push delta — D-07 requires direct apply without network round-trip, so the payload must be complete.

Output: Corrected PushNotificationService.java payload template, corrected TimerSyncService.java convertActionToStateDto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-cross-sync/04-CONTEXT.md
@.planning/phases/04-cross-sync/04-RESEARCH.md
@back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java
@back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java
@back-end/src/main/java/com/sparkage/timebeam/presentation/dto/TimerStateDto.java
@back-end/src/main/java/com/sparkage/timebeam/infrastructure/persistence/TimerState.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix push payload template to include autoStartNextSession and shortBreaksCompleted</name>
  <files>back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java</files>
  <read_first>
    - back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java (current payload template at line 151-163)
    - back-end/src/main/java/com/sparkage/timebeam/infrastructure/persistence/TimerState.java (entity field names: autoStartNext, shortBreaksCompleted)
    - back-end/src/main/java/com/sparkage/timebeam/presentation/dto/TimerStateDto.java (DTO getter names: getAutoStartNextSession(), getShortBreaksCompleted())
  </read_first>
  <behavior>
    - Test 1: sendTimerSyncPush with autoStartNextSession=true produces payload containing "autoStartNextSession":true
    - Test 2: sendTimerSyncPush with shortBreaksCompleted=3 produces payload containing "shortBreaksCompleted":3
    - Test 3: Full payload string contains all 12 timer state fields (phase, remainingSeconds, isRunning, startTimestamp, pauseTimestamp, workDuration, breakDuration, longBreakDuration, autoStartNextSession, shortBreaksCompleted, deviceId, lastModifiedTimestamp)
  </behavior>
  <action>
    In PushNotificationService.java sendTimerSyncPush() method, modify the String.format payload template (currently at line 151-153) to add two fields: autoStartNextSession (boolean) and shortBreaksCompleted (integer).

    Current template format string ends with: ..., "deviceId":"%s","lastModifiedTimestamp":%f}"
    New template must include between longBreakDuration and deviceId: "autoStartNextSession":%s,"shortBreaksCompleted":%d

    Add two format arguments after the longBreakDuration argument:
    - state.getAutoStartNextSession() != null ? state.getAutoStartNextSession() : false (for the boolean %s)
    - state.getShortBreaksCompleted() != null ? state.getShortBreaksCompleted() : 0 (for the integer %d)

    Follow the existing null-coalescing pattern used for workDuration/breakDuration/longBreakDuration on lines 159-161.

    Per D-07 (APNs delta apply without round-trip) and D-08 (push payload must include lastModifiedTimestamp — already done).
  </action>
  <verify>
    <automated>cd back-end && mvn test -Dtest=TimerSyncServiceTest -q</automated>
  </verify>
  <done>
    Payload template String.format includes autoStartNextSession (%s) and shortBreaksCompleted (%d) format specifiers between longBreakDuration and deviceId. Two new null-safe arguments added for these fields, following the existing ternary pattern.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix convertActionToStateDto to read autoStartNextSession from entity</name>
  <files>back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java</files>
  <read_first>
    - back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java (convertActionToStateDto at line 427-443)
    - back-end/src/main/java/com/sparkage/timebeam/infrastructure/persistence/TimerState.java (entity has isAutoStartNext() / getAutoStartNext() methods)
    - back-end/src/main/java/com/sparkage/timebeam/presentation/dto/TimerStateDto.java (constructor parameter order — autoStartNextSession is 7th parameter)
  </read_first>
  <behavior>
    - Test 1: convertActionToStateDto with entity autoStartNext=true produces DTO with autoStartNextSession=true
    - Test 2: convertActionToStateDto with entity autoStartNext=false produces DTO with autoStartNextSession=false
  </behavior>
  <action>
    In TimerSyncService.java convertActionToStateDto() method (line 427-443), the 7th argument to TimerStateDto constructor is currently hardcoded as `false` for autoStartNextSession. Replace `false` with `state.isAutoStartNext()` or `state.getAutoStartNext()` — whichever boolean accessor the TimerState entity actually exposes (verify in TimerState.java).

    The existing call passes `state.getShortBreaksCompleted()` as the 8th argument — this is correct and needs no change.

    This fix ensures that when a timer action triggers a push notification, the converted DTO carries the real autoStartNextSession value from the persisted entity, not a hardcoded false.
  </action>
  <verify>
    <automated>cd back-end && mvn test -Dtest=TimerSyncServiceTest -q</automated>
  </verify>
  <done>
    Line 435 in TimerSyncService.java convertActionToStateDto no longer hardcodes false for autoStartNextSession. It reads the value from the TimerState entity using the correct accessor method (isAutoStartNext or getAutoStartNext).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Backend API | Timer action/state requests cross network; JWT-authenticated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | POST /api/sessions/timer/action | mitigate | JWT-authenticated endpoints; AuthManager validates token before TimerSyncService processes action |
| T-04-02 | Repudiation | PushNotificationService sendTimerSyncPush | mitigate | Payload includes deviceId (updatedByDeviceId) for traceability; backend logs action source device |
| T-04-03 | Tampering | APNs push payload in transit | accept | APNs TLS is built-in; payload not encrypted beyond transport layer. Timer state is non-sensitive (no PII). ASVS L1 does not require payload encryption for this data class. |
</threat_model>

<verification>
1. Build succeeds: `cd back-end && mvn clean compile -q`
2. Tests pass: `cd back-end && mvn test -q`
3. Grep payload template: `grep -c 'autoStartNextSession' back-end/src/main/java/com/sparkage/timebeam/infrastructure/external/PushNotificationService.java` returns >= 1
4. Grep entity accessor: `grep -c 'getAutoStartNext\|isAutoStartNext' back-end/src/main/java/com/sparkage/timebeam/application/service/TimerSyncService.java` returns >= 1 in convertActionToStateDto
</verification>

<success_criteria>
- PushNotificationService.java payload template includes autoStartNextSession and shortBreaksCompleted fields with null-safe values
- TimerSyncService.java convertActionToStateDto reads autoStartNextSession from entity (not hardcoded false)
- Backend test suite passes: `cd back-end && mvn test`
</success_criteria>

<output>
After completion, create `.planning/phases/04-cross-sync/04-01-SUMMARY.md`
</output>
