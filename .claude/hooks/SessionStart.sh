#!/usr/bin/env bash
# Load TimeBeam project context on startup
echo '{"systemMessage": "Load TimeBeam project context: CLAUDE.md, .obsidian/projects/, .planning/STATE.md. Remember: API_BASE_URL is in project.pbxproj not Info.plist. Backend actionType needs @JsonAlias for iOS action field. macOS Keychain needs com.apple.security.keychain.access-groups entitlement."}'
