# Timezone Findings

## Backend:
- Uses Instant (which is UTC by default)
- Converts to Unix timestamp: toEpochMilli() / 1000.0
- Stores in database as timestamp with timezone

## Frontend:
- Uses Double for timestamps (Unix time in seconds)
- JSONDecoder with iso8601 strategy
- Date objects (which can have timezone issues)

## Database:
- timestamp columns with timezone information
- Need to verify proper UTC storage

## Issues Found:
- Need to verify all conversions maintain UTC
- Check for any local timezone usage
- Ensure JSON serialization preserves UTC